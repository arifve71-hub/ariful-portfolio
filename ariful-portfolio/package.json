{
  "name": "ariful-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html videos.html *.png *.jpg *.jpeg *.svg dist/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}